{
  "version": 2,
  "builds": [
    {
      "src": "DjangoApp/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9",
        "buildCommand": "npm install && npm run build:css && pip install -r requirements.txt && python manage.py collectstatic --noinput"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "DjangoApp/wsgi.py"
    }
  ]
}